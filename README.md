# Serene Liu — 個人履歷網站

一頁式個人網站 + PDF 履歷。純靜態,無需 build。

## 檔案
- `index.html` — 網站主頁(單一檔案,圖片已內嵌)
- `serene-liu-resume.pdf` — PDF 履歷(頁面「下載 PDF 履歷」按鈕的目標)

## 更新方式
修改檔案後 push 到 main,Cloudflare Pages 會自動重新部署。
